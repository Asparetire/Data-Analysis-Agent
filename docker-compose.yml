services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # No host port — nginx proxies /api to this container. Exposing 8000
    # would let clients bypass the reverse proxy (and its security headers).
    expose:
      - "8000"
    environment:
      # LLM provider — switch with LLM_PROVIDER=anthropic to use Claude.
      - LLM_PROVIDER=${LLM_PROVIDER:-openai}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-}
      - ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL:-}
      # Phase 4A: auth — JWT_SECRET must be ≥32 bytes, override in .env.
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required (≥32 bytes)}
      - ACCESS_TOKEN_TTL_MINUTES=${ACCESS_TOKEN_TTL_MINUTES:-15}
      - REFRESH_TOKEN_TTL_DAYS=${REFRESH_TOKEN_TTL_DAYS:-7}
      - MIGRATION_ADMIN_EMAIL=${MIGRATION_ADMIN_EMAIL:-admin@local.invalid}
      - MIGRATION_ADMIN_PASSWORD=${MIGRATION_ADMIN_PASSWORD:?MIGRATION_ADMIN_PASSWORD is required}
      # Phase 4B: rate limits
      - RATE_LIMIT_PER_USER_PER_MINUTE=${RATE_LIMIT_PER_USER_PER_MINUTE:-60}
      - RATE_LIMIT_PER_IP_PER_MINUTE=${RATE_LIMIT_PER_IP_PER_MINUTE:-20}
      # Phase 5B: observability
      - LOG_FORMAT=${LOG_FORMAT:-json}
      # Infra
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=sqlite:///app/data/main.db
      - DATA_DIR=/app/data
      - CORS_ORIGINS=${CORS_ORIGINS:-["http://localhost"]}
    volumes:
      - backend_data:/app/data
    depends_on:
      - redis

  # Single ingress — serves frontend static (built into the image) and
  # proxies /api/* to the backend. TLS is terminated upstream (cloud LB /
  # Cloudflare), so this only listens on 80.
  nginx:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  redis:
    image: redis:7-alpine
    expose:
      - "6379"
    ports:
      # Keep host port for local dev (pytest / manual redis-cli). Production
      # deployments should drop this and only expose inside the compose network.
      - "6379:6379"
    volumes:
      - redis_data:/data

  # MinIO — S3-compatible local object storage, used as the backup target.
  # Console at :9001 lets you browse uploaded backups in the browser.
  minio:
    image: minio/minio:RELEASE.2024-10-13T13-34-11Z
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER:-minioadmin}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required (≥8 chars)}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # One-shot backup runner. Triggered manually:
  #   docker compose run --rm backup
  # Scheduling (cron / K8s CronJob / systemd timer) lives outside compose.
  # Reuses the backend image so the script has the same deps (boto3, redis,
  # sqlalchemy) as the app — no separate image to maintain.
  backup:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: python scripts/backup.py
    environment:
      - DATABASE_URL=sqlite:///app/data/main.db
      - REDIS_URL=redis://redis:6379/0
      - MINIO_ENDPOINT=http://minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ROOT_USER:-minioadmin}
      - MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
      - MINIO_BUCKET=${MINIO_BUCKET:-data-analysis-backups}
      - BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-7}
    volumes:
      - backend_data:/app/data
      # Read-only access to redis dump.rdb so backup.py can copy it.
      - redis_data:/data:ro
    depends_on:
      - backend
      - redis
      - minio
    profiles:
      # Don't start on `docker compose up` — only on explicit `--profile backup`.
      - backup

volumes:
  backend_data:
  redis_data:
  minio_data:
