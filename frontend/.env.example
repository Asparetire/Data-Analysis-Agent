# 前端构建时 / 运行时需要的环境变量（Vite 用 VITE_ 前缀暴露给客户端）
# 目前项目未在浏览器侧调用 LLM，所有 LLM 调用都走后端，因此这里无需 OPENAI_API_KEY。
# 如果之后在前端直接接入 SDK，把 key 改为 VITE_OPENAI_API_KEY 等。

# 后端 API 地址（Vite dev server 启动时会用 /api 代理本地 8000，一般无需改）
# VITE_API_BASE_URL=/api/v1
