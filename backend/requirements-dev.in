# dev-only dependencies. Install with:
#   pip install -r requirements-dev.txt
# Tests use the in-process FastAPI TestClient (httpx) and an in-memory
# Redis fake (fakeredis) so no live services are required to run them.
-c requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
fakeredis>=2.21

# Lint / format / hooks
ruff>=0.6
black>=24.0
pre-commit>=3.8
